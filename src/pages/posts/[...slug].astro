---
import { getCollection, render } from 'astro:content';
import Layout from '@/layouts/Layout.astro';
import CommentSection from '@/components/CommentSection';
import { fmtDateKr } from '@/utils/date';

export async function getStaticPaths() {
  const posts = await getCollection('posts', ({ data }) => data.published !== false);
  return posts.map((post) => {
    const permalink = post.data.permalink || '';
    const slug = permalink.startsWith('/') ? permalink.slice(1) : permalink;
    return {
      params: { slug },
      props: { post },
    };
  });
}

const { post } = Astro.props;
const { Content, headings } = await render(post);

const toc = headings.filter(h => h.depth <= 3);
const rawHeroImage = post.data.heroImage && post.data.heroImage.trim() ? post.data.heroImage : undefined;

// Resolve heroImage: absolute paths/URLs used directly, relative paths resolved via glob
const contentImages = import.meta.glob<{ default: ImageMetadata }>(
  '/src/content/posts/**/*.{png,jpg,jpeg,gif,webp}',
  { eager: true }
);

let heroImage: string | undefined;
if (rawHeroImage) {
  if (rawHeroImage.startsWith('/') || rawHeroImage.startsWith('http')) {
    heroImage = rawHeroImage;
  } else if (rawHeroImage.startsWith('./')) {
    const postDir = post.id.includes('/') ? post.id.split('/')[0] : post.id;
    const imageName = rawHeroImage.slice(2);
    const key = `/src/content/posts/${postDir}/${imageName}`;
    const mod = contentImages[key];
    if (mod) {
      heroImage = mod.default.src;
    }
  }
}

let postSlug = post.data.permalink || post.id;
if (postSlug.startsWith('/')) postSlug = postSlug.slice(1);
const ogImage = heroImage || `/og/posts/${postSlug}.png`;
---

<Layout
  title={post.data.title}
  description={post.data.description}
  image={ogImage}
  ogType="article"
  publishedTime={post.data.date.toISOString()}
  tags={post.data.tags}
>
  <article class="article">
    {heroImage && (
      <div class="hero-image">
        <img src={heroImage} alt={post.data.heroImageAlt || post.data.title} />
      </div>
    )}

    <header class="article-header">
      <h1 class="article-title">{post.data.title}</h1>

      {post.data.tags.length > 0 && (
        <div class="article-tags">
          {post.data.tags.map(tag => (
            <a href={`/posts/?tag=${tag}`} class="tag">{tag}</a>
          ))}
        </div>
      )}

      <div class="article-meta">
        <time datetime={post.data.date.toISOString()}>
          {fmtDateKr(post.data.date)}
        </time>
      </div>

      {post.data.description && (
        <p class="article-description">{post.data.description}</p>
      )}
    </header>

    {toc.length > 2 && (
      <nav class="toc" aria-label="Table of Contents">
        <details>
          <summary>목차</summary>
          <ul class="toc-list">
            {toc.map(heading => (
              <li class={`toc-item depth-${heading.depth}`}>
                <a href={`#${heading.slug}`}>{heading.text}</a>
              </li>
            ))}
          </ul>
        </details>
      </nav>
    )}

    <div class="article-content prose">
      <Content />
    </div>

    <footer class="article-footer">
      <a href="/" class="back-link">← 목록으로 돌아가기</a>
    </footer>

    <!-- Comments -->
    <section class="comments">
      <CommentSection client:load postSlug={postSlug} postType="posts" />
    </section>
  </article>
</Layout>

<style lang="scss">
  @use '../../styles/abstracts' as *;

  .article {
    max-width: var(--container-narrow);
    margin: 0 auto;
    padding: var(--space-8) var(--space-6);
  }

  .hero-image {
    margin: 0 -1.5rem 2rem;

    img {
      width: 100%;
      height: auto;
      max-height: 400px;
      object-fit: cover;
    }
  }

  .article-header {
    margin-bottom: var(--space-8);
    padding-bottom: var(--space-6);
    border-bottom: 1px solid var(--border-default);
  }

  .article-title {
    @include display-2;
    font-weight: 700;
    color: var(--text-primary);
    line-height: 1.3;
    margin: 0 0 var(--space-4);
    word-break: keep-all;
  }

  .article-tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    margin-bottom: var(--space-3);
  }

  .tag {
    padding: var(--space-1) var(--space-3);
    background: var(--surface-raised);
    border-radius: var(--radius-sm);
    @include caption;
    color: var(--accent-default);
    text-decoration: none;
    transition: background-color var(--duration-base) var(--easing-standard), color var(--duration-base) var(--easing-standard), border-color var(--duration-base) var(--easing-standard);

    &:hover {
      background: var(--accent-default);
      color: white;
    }
  }

  .article-meta {
    color: var(--text-muted);
    @include body-sm;
    margin-bottom: var(--space-4);
  }

  .article-description {
    @include body-lg;
    color: var(--text-secondary);
    line-height: 1.7;
    margin: 0;
  }

  .toc {
    margin-bottom: var(--space-8);
    padding: var(--space-4) var(--space-5);
    background: var(--surface-raised);
    border-radius: var(--radius-md);
    border-left: 3px solid var(--accent-default);

    summary {
      font-weight: 600;
      cursor: pointer;
      color: var(--text-secondary);
      @include body-sm;
    }
  }

  .toc-list {
    list-style: none;
    padding: 0;
    margin: var(--space-3) 0 0;
  }

  .toc-item {
    margin: 0.375rem 0;

    a {
      color: var(--text-secondary);
      text-decoration: none;
      @include body-sm;

      &:hover {
        color: var(--accent-default);
      }
    }

    &.depth-2 {
      padding-left: var(--space-4);
    }

    &.depth-3 {
      padding-left: var(--space-8);
      @include caption;
    }
  }

  .article-content {
    /* 폭은 .article(--container-narrow)이 결정 — TOC와 같은 폭으로 정렬.
       상한 980px 기준 본문 17px에서 한 줄 ~55자로 CPL 범위 안에 들어옵니다. */
    @include body-lg;
    line-height: 1.85;
    color: var(--text-secondary);
    word-break: keep-all;

    :global(h2) {
      @include heading-1;
      font-weight: 700;
      margin: var(--space-12) 0 var(--space-4);
      padding-bottom: var(--space-2);
      border-bottom: 1px solid var(--border-default);
      color: var(--text-primary);
    }

    :global(h3) {
      @include heading-2;
      font-weight: 600;
      margin: 2rem 0 0.75rem;
      color: var(--text-primary);
    }

    :global(h4) {
      @include heading-3;
      font-weight: 600;
      margin: var(--space-6) 0 var(--space-2);
      color: var(--text-primary);
    }

    :global(p) {
      margin: 1.25rem 0;
    }

    :global(ul), :global(ol) {
      margin: var(--space-4) 0;
      padding-left: var(--space-6);
    }

    :global(li) {
      margin: var(--space-2) 0;
    }

    :global(blockquote) {
      margin: var(--space-6) 0;
      padding: var(--space-4) var(--space-6);
      border-left: 4px solid var(--accent-default);
      background: var(--surface-raised);
      color: var(--text-secondary);
      border-radius: 0 0.5rem 0.5rem 0;

      :global(p) {
        margin: 0;
      }
    }

    :global(img) {
      max-width: 100%;
      height: auto;
      border-radius: var(--radius-md);
      margin: 1.5rem auto;
      display: block;
    }

    :global(a) {
      color: var(--accent-default);
      text-decoration: underline;
      text-underline-offset: 2px;
    }

    :global(code:not(pre code)) {
      padding: 0.15rem 0.4rem;
      background: var(--code-inline-bg, rgba(0, 0, 0, 0.05));
      border-radius: var(--radius-sm);
      font-size: 0.875em;
      font-family: 'SF Mono', 'Monaco', 'Inconsolata', 'Fira Mono', monospace;
      color: var(--code-inline-color, var(--state-error));
    }

    :global(.expressive-code) {
      margin: var(--space-6) 0;
    }

    :global(pre) {
      margin: var(--space-6) 0;
      border-radius: var(--radius-md);
      overflow-x: auto;
    }

    :global(table) {
      width: 100%;
      border-collapse: collapse;
      margin: var(--space-6) 0;
      @include body-sm;

      :global(th), :global(td) {
        padding: var(--space-3);
        border: 1px solid var(--border-default);
        text-align: left;
      }

      :global(th) {
        background: var(--surface-raised);
        font-weight: 600;
      }
    }

    :global(hr) {
      margin: var(--space-8) 0;
      border: none;
      border-top: 1px solid var(--border-default);
    }

    :global(strong) {
      font-weight: 600;
      color: var(--text-primary);
    }
  }

  .article-footer {
    margin-top: var(--space-12);
    padding-top: var(--space-6);
    border-top: 1px solid var(--border-default);
  }

  .back-link {
    color: var(--accent-default);
    font-weight: 500;
    text-decoration: none;

    &:hover {
      text-decoration: underline;
    }
  }

  .comments {
    margin-top: var(--space-12);
    padding-top: var(--space-8);
    border-top: 1px solid var(--border-default);
  }

  @include belowTablet {
    .article-title {
      @include heading-1;
    }

    .article-content {
      @include body;

      :global(h2) {
        @include heading-2;
      }

      :global(h3) {
        @include heading-3;
      }
    }

    .hero-image {
      margin: 0 -1.5rem 1.5rem;
    }
  }
</style>
