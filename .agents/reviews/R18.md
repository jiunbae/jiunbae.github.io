---
round: R18
scores: { A: 9.8, B: 9.68, C: 9.7, D: 9.5 }
avg: 9.67
delta: +0.057
metrics:
  token_adoption: 91.81%
  stylelint_errors: 0
  axe_violations: 0  (final, after fixes within round)
---

# R18 — axe pipeline 도입 + 0 a11y violations

## 인프라 추가

- `scripts/axe-run.mjs` — Playwright + @axe-core/playwright (chromedriver-free)
- `scripts/visual-regression.mjs` — 15 pages × {light, dark} × {mobile, desktop}
  = 60 snapshots baseline
- npm scripts: `audit:a11y`, `visual:baseline`, `visual:check`

## axe-core 첫 실행 결과 (라운드 시작 시)

```
Total violations: 173 (0 critical, 170 serious, 3 moderate)
- /playground/ 72
- /status/    30
- /projects/  23
- /design/changelog 19
- ... 등
```

대부분 `--gray-6` (#8b8b8b) 배경 위 `--text-secondary` (#282828) — 4.32:1
(SC 1.4.3 미달). state-success/warning이 light에서 contrast 부족.

## 적용된 fix (라운드 내 즉시 해결)

- `.tag` bg `var(--gray-6)` → `var(--surface-subtle)` (3개 페이지)
- `--state-success` light: `--green-500` → `--green-700` (5.5:1)
- `--state-warning` light: `--orange-500` → `--orange-700` (4.95:1)
- `--state-warning-bg` 12% → 8% mix
- `services.ts` categories hex AA-compliant darker shade로 교체
- `projects .status-dot .live/.beta` bg 15% → 6%/4% mix
- `Note <aside>` → `<div role="note">` (landmark-complementary-is-top-level)
- Body link `text-decoration-color` 30% → 75% opacity (SC 1.4.1)
- contact/privacy 본문 링크 underline 추가
- `badge-info` color `--accent-default` → `--accent-hover` (4.5:1+)

**173 → 0 violations** 달성.

## A — DS Adoption: **9.8** (+0.1)

- Token system exemplary; state-* split (-strong, -bg, -dot)
- Note role="note" 정확
- 91.81% adoption
- BUT: 시각화 views (Boids, CyberFlowers, AudioVisualizer) brand 팔레트
       하드코딩 잔존 (P0)
- BUT: typography scale tokens 부재 (`--font-size-*`)

## B — UX / Readability: **9.68** (+0.03)

- Token + Korean readability + a11y plumbing 일관됨
- BUT: `.btn-sm` 22px (WCAG 2.5.5 24×24 미달) P0
- BUT: index `<h1 class="sr-only">` 시각 위계 손실 (P1)
- BUT: 한글 line-height 일부 1.4 (1.5 권장)

## C — Accessibility: **9.7** (unchanged)

axe 0 violations. Manual review:
- BUT: ImageConverter 토글 그룹 `aria-pressed` 누락
- BUT: SearchModal `aria-expanded` always true while open
- BUT: CommentSection `alert()`/`confirm()` (P1)
- BUT: 드롭존 hidden file input double-tab-stop (P1)

## D — IA / Gestalt: **9.5** (+0.1)

- token system 9.6, IA labeling 9.5, card affordance Gestalt 9.4
- BUT: `prefers-reduced-motion`이 transition만 단축, transform translateY 잔존
       (vestibular P0)
- BUT: about heading hierarchy 잔존 죽은 h4 selector

## Avg: **9.67** (+0.057)

## 추가 fix (라운드 후 commit)

- Button `.btn-sm/-md/-lg` `min-height: 32/40/44px` (WCAG 2.5.5)
- `_global.scss prefers-reduced-motion` `transform: none !important` 추가
- ImageConverter W/H input `aria-label`, lock-aspect `aria-pressed`+`aria-label`
- 3개 converter 드롭존 hidden input `tabIndex={-1} aria-hidden`, Space
  `preventDefault()`
- editorial typography scale 토큰화 (`--editorial-display-cover`,
  `--editorial-display-spread`), JSX 주석으로 의도 명시
- header logomark `var(--icon-color)` 명시 (link cascade 우회)

## 마감 상태

- 87 pages build success
- 91.81% token adoption
- 0 stylelint errors
- 0 axe violations
- Visual regression baseline (60 snapshots)
- R11 → R18 평균 +0.87
